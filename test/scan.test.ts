import{describe,it,expect}from"vitest";import{scanConfig}from"../src/scanner.js";
describe("scanConfig",()=>{it("safe",()=>{expect(scanConfig({name:"s",command:"node",args:["s.js"]}).passed).toBe(true);});it("unpinned",()=>{expect(scanConfig({name:"x",command:"npx",args:["-y","pkg"]}).findings.some(f=>f.rule==="mcp:unpinned-npx")).toBe(true);});it("pinned ok",()=>{expect(scanConfig({name:"x",command:"npx",args:["-y","pkg@1.0"]}).findings.some(f=>f.rule==="mcp:unpinned-npx")).toBe(false);});it("secret",()=>{expect(scanConfig({name:"x",command:"n",args:[],env:{KEY:"sk-12345678901234567890"}}).findings.some(f=>f.rule==="mcp:inline-secret")).toBe(true);});it("plaintext",()=>{expect(scanConfig({name:"x",url:"http://example.com"}).findings.some(f=>f.rule==="mcp:plaintext-transport")).toBe(true);});it("localhost ok",()=>{expect(scanConfig({name:"x",url:"http://localhost:3000"}).findings.some(f=>f.rule==="mcp:plaintext-transport")).toBe(false);});});